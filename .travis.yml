language: node_js

node_js:
    - "14"
before_install: cd nodejs
install:
    - npm install
after_success:
- docker login --username=$DOCKERUSER --password-stdin=$DOCKERPASSWORD
- docker build --rm -t $DOCKERUSER/$DEPLOYIMAGE
- docker push $DOCKERUSER/$DEPLOYIMAGE:least